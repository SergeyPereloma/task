import sys
import time

def main():
    print("Starting the count...")

    for i in range(1, 1001):
        if i % 2 == 1:
            print(f"Count: {i}", file=sys.stderr)
        else:
            print(f"Count: {i}")
if __name__ == "__main__":
    main()
